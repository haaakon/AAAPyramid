#include "Pods-UITextField-Blocks.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/UITextField-Blocks" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/UITextField-Blocks"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}